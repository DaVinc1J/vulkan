// Update record_command_buffer function to change object type condition
if (obj->type != SOLAR_OBJECT_TYPE_PLAIN) {
    // Existing code for handling non-PLAIN objects
}
else {
    // Existing code for rendering PLAIN objects
}